export interface Competitor{id:string;displayName:string;fictional:true;status:'ACTIVE'|'INACTIVE';budgetMinor:number;reservedMinor:number;strategy:'VALUE'|'AGGRESSIVE'|'CAUTIOUS';privateValuationMinorPerM3:number;valuationErrorBasisPoints:number;createdTimestamp:number;sourceEventIds:string[]}
export interface Auction{id:string;displayName:string;auctionType:'PREPARED_ROUNDWOOD'|'STANDING_TIMBER_PLACEHOLDER';status:'OPEN'|'CLOSED'|'CANCELLED';volumeMilliM3:number;openingRateMinorPerM3:number;incrementMinorPerM3:number;depositBasisPoints:number;lateWindowSeconds:number;extensionSeconds:number;closeTimestamp:number;paymentDeadlineSeconds:number;removalDeadlineSeconds:number;pendingWinnerCommitmentId:string;winningBidId?:string;winnerId?:string;winningRateMinorPerM3?:number;winningTotalMinor?:number;resultPublished:boolean;createdTimestamp:number;sourceEventIds:string[]}
export interface AuctionRegistration{id:string;auctionId:string;bidderId:string;bidderType:'COMPANY'|'COMPETITOR';depositMinor:number;depositCommitmentId?:string;status:'ACTIVE'|'LOST_RELEASED'|'WON';createdTimestamp:number;sourceEventIds:string[]}
export interface AuctionBid{id:string;auctionId:string;bidderId:string;bidderType:'COMPANY'|'COMPETITOR';rateMinorPerM3:number;proxyMaximumMinorPerM3:number;totalMinor:number;createdTimestamp:number;sourceEventIds:string[]}
export interface AuctionResult{auctionId:string;winnerId?:string;winningBidId?:string;winningRateMinorPerM3?:number;winningTotalMinor?:number;closedTimestamp:number;publicBidderIds:string[]}
export interface AuctionSnapshot{appliedEventIds:string[];competitors:Competitor[];auctions:Auction[];registrations:AuctionRegistration[];bids:AuctionBid[];results:AuctionResult[]}
