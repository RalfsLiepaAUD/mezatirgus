import type{SimulationEngine}from'../core/engine.js';export function auctionList(e:SimulationEngine){return structuredClone(e.auctions.snapshot().auctions)}export function auctionDetail(e:SimulationEngine,id:string){const s=e.auctions.snapshot(),a=s.auctions.find(x=>x.id===id);if(!a)return undefined;return structuredClone({auction:a,registrations:s.registrations.filter(x=>x.auctionId===id).map(({depositCommitmentId,...x})=>x),bids:s.bids.filter(x=>x.auctionId===id),result:s.results.find(x=>x.auctionId===id)})}export function publicAuctionResults(e:SimulationEngine){return structuredClone(e.auctions.snapshot().results)}export function competitorDirectory(e:SimulationEngine){return e.auctions.snapshot().competitors.map(({budgetMinor,reservedMinor,privateValuationMinorPerM3,valuationErrorBasisPoints,strategy,...visible})=>structuredClone(visible))}
