import{command,SimulationEngine}from'../core/engine.js';import{createSave,loadSave}from'../persistence/save.js';import{createSnapshot}from'../persistence/snapshot.js';import{routeDetail,schematicGraph}from'../routing/read-models.js';const e=new SimulationEngine({seed:'map-demo',configurationBundleVersion:'1',configurationHash:'demo',scenarioId:'scenario.first_full',clock:{paused:false}});const snapshot=createSnapshot(e);const go=(n:string,t:string,p:any)=>{const r=e.execute(command(n,t,e,p));console.log(n,r.accepted?'accepted':'rejected');return r;};for(const[n,name]of[['R','Rīga'],['C','Cēsis'],['Y','Mežtirgus yard']]as const)go(n,'CreateLocation',{displayName:name,countryCode:'LV',regionCode:'VIDZEME',roles:['REGIONAL_NODE']});const[a,b,c]=e.routing.snapshot().locations.map(x=>x.id);go('AB','CreateRouteEdge',{fromLocationId:a,toLocationId:b,accessClass:'PAVED',distanceMetres:100000,travelSeconds:7200});go('BC','CreateRouteEdge',{fromLocationId:b,toLocationId:c,accessClass:'GRAVEL',distanceMetres:20000,travelSeconds:2400});go('AC','CreateRouteEdge',{fromLocationId:a,toLocationId:c,accessClass:'PAVED',distanceMetres:150000,travelSeconds:12000});console.log('initial route',routeDetail(e,a!,c!));go('BAD','CreateRouteEdge',{fromLocationId:'missing',toLocationId:c,accessClass:'PAVED',distanceMetres:1,travelSeconds:1});go('CLOSE','ScheduleRouteEdgeAccess',{edgeId:'EDGE-000002',accessState:'BLOCKED',atTimestamp:1800});go('STALE','ScheduleRouteEdgeAccess',{edgeId:'EDGE-000002',accessState:'BLOCKED',atTimestamp:1801});e.advanceUntil(1801);console.log('after closure',routeDetail(e,a!,c!));const save=createSave(e,snapshot),loaded=loadSave(save);console.log('summary',JSON.stringify({graph:schematicGraph(loaded),stateChecksum:e.stateChecksum(),loadedChecksum:loaded.stateChecksum(),eventLogChecksum:e.eventLogChecksum(),loadedEventLogChecksum:loaded.eventLogChecksum(),eventLogsMatch:e.eventLogChecksum()===loaded.eventLogChecksum(),saveChecksum:save.saveChecksum,replayMatches:e.stateChecksum()===loaded.stateChecksum()},null,2));