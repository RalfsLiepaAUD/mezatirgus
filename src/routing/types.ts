export type AccessClass='PAVED'|'GRAVEL'|'FOREST_ROAD'|'SEA';export type AccessState='OPEN'|'BLOCKED'|'CLOSED';export interface Location{id:string;schemaVersion:number;displayName:string;countryCode:string;regionCode:string;roles:string[];latitudeE6?:number;longitudeE6?:number;status:'ACTIVE'|'INACTIVE';createdTimestamp:number;updatedTimestamp:number;sourceIds:string[];}export interface RouteEdge{id:string;schemaVersion:number;fromLocationId:string;toLocationId:string;accessClass:AccessClass;accessState:AccessState;distanceMetres:number;travelSeconds:number;directed:true;createdTimestamp:number;updatedTimestamp:number;sourceIds:string[];}export interface RoutingSnapshot{appliedEventIds:string[];locations:Location[];edges:RouteEdge[];}export interface RouteQuote{fromLocationId:string;toLocationId:string;edgeIds:string[];locationIds:string[];distanceMetres:number;travelSeconds:number;accessClasses:AccessClass[];}