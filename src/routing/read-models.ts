import type{SimulationEngine}from'../core/engine.js';export function locationList(e:SimulationEngine){return e.routing.snapshot().locations;}export function schematicGraph(e:SimulationEngine){const s=e.routing.snapshot();return{locations:s.locations,edges:s.edges};}export function routeDetail(e:SimulationEngine,from:string,to:string){try{return{ok:true as const,quote:e.routing.route(from,to),reason:null};}catch(error){return{ok:false as const,quote:null,reason:error instanceof Error?error.message:'NO_ROUTE'};}}