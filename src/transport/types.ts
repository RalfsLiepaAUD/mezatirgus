export interface Carrier{ id:string; schemaVersion:1; configId:string; displayName:string; fictional:true; status:'ACTIVE'|'INACTIVE'; capacityMilliM3:number; baseCalloutMinor:number; distanceRateMinorPerKm:number; volumeRateMinorPerM3:number; pickupDelaySeconds:number; paymentTermsSeconds:number; disruptionChanceBasisPoints:number; disruptionDelaySeconds:number; disruptionSurchargeMinor:number; createdTimestamp:number; sourceEventIds:string[] }
export interface CarrierQuote{ id:string; carrierId:string; companyId:string; loadId:string; originLocationId:string; destinationLocationId:string; routeEdgeIds:string[]; distanceMetres:number; travelSeconds:number; volumeMilliM3:number; urgencyBasisPoints:number; breakdown:{baseCalloutMinor:number;distanceCostMinor:number;volumeCostMinor:number;urgencyAdjustmentMinor:number;finalTotalMinor:number}; createdTimestamp:number; expiryTimestamp:number; status:'OPEN'|'ACCEPTED'|'EXPIRED'|'CANCELLED'; sourceEventIds:string[] }
export interface TransportJob{ id:string; quoteId:string; carrierId:string; companyId:string; loadId:string; ownerCompanyId:string; originLocationId:string; destinationLocationId:string; routeEdgeIds:string[]; volumeMilliM3:number; status:'PLANNED'|'WAITING_CONFIRMATION'|'DISPATCHED'|'IN_TRANSIT'|'ARRIVED'|'UNLOADED'|'COMPLETED'|'CANCELLED'|'FAILED'; confirmedTimestamp?:number; pickupTimestamp?:number; plannedArrivalTimestamp?:number; actualArrivalTimestamp?:number; disruptionDelaySeconds:number; disruptionSurchargeMinor:number; finalCostMinor:number; payableId?:string; createdTimestamp:number; sourceEventIds:string[] }
export interface TransportSnapshot{appliedEventIds:string[];carriers:Carrier[];quotes:CarrierQuote[];jobs:TransportJob[]}
