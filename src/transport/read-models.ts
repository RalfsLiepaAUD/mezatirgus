import type{SimulationEngine}from'../core/engine.js';export function carrierDirectory(e:SimulationEngine){return structuredClone(e.transport.snapshot().carriers)}export function carrierQuoteView(e:SimulationEngine,id:string){const q=e.transport.quote(id);return q?structuredClone(q):undefined}export function transportJobView(e:SimulationEngine,id:string){const j=e.transport.job(id);return j?structuredClone(j):undefined}export function transportBoard(e:SimulationEngine){return structuredClone(e.transport.snapshot().jobs)}
